version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - pip install -r requirements.txt -t .
  pre_build:
    commands:
      - pytest -q
  build:
    commands:
      - zip -q -r lambda.zip .
artifacts:
  files:
    - lambda.zip
